#include <iostream>

using namespace std;

int main()
{
    int one, two;
    string temp;
    cin >> one >> two;
    for (one; one>0; one--)
    {
        cin >> temp;
    }
    
    cout << two;
}